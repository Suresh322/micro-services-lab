spring:
  cloud:
    config:
      enabled: false
  datasource:
    url: jdbc:h2:mem:loans-test;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

build:
  version: "3.0"

loans:
  message: "Welcome to EazyBank loans related test APIs"
  contactDetails:
    name: "Test Developer"
    email: "test@eazybank.com"
  onCallSupport:
    - (000) 000-0000
